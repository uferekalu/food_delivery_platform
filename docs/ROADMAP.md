# Roadmap

Phased build order. Each phase is one `feature/FDP-<n>-<description>` branch, pushed as a PR
against `main` and merged (see `docs/ENGINEERING_RULES.md` for the current branch/PR/merge
workflow — nothing is ever pushed directly to `main`).

**Next available ticket number: FDP-69**

Update the number above every time a new ticket branch is created (the
`new-feature-branch` skill in `.claude/skills/` reads this file to pick the next number).

## Phases

| # | Branch suffix | Scope | Status |
|---|---|---|---|
| FDP-1 | `foundation-docs` | Product guide, architecture doc, roadmap, engineering rules, root CLAUDE.md, branch-creation skill | ✅ Done |
| FDP-2 | `frontend-design-system` | Next.js app scaffold, Tailwind config, design tokens, full hand-built UI kit + component showcase page | ✅ Done |
| FDP-3 | `backend-foundation` | NestJS app scaffold, config/env validation, MongoDB connection, health check, logging, error handling, Swagger | ✅ Done |
| FDP-4 | `auth` | Backend auth (register/login/refresh/verify/reset, role guards) + frontend auth pages/session handling | ✅ Done |
| FDP-5 | `restaurants-menu` | Restaurant + menu CRUD (backend), Cloudinary uploads, customer browse/menu pages **+ restaurant owner dashboard** (create/edit restaurant, manage menu) — folded in because no later phase owns this UI | ✅ Done |
| FDP-6 | `platform-hardening` | Responsive design retrofit (mobile-first audit + fixes across every existing page/UI-kit component), Redux session reauth-on-401 (silent refresh-and-retry, matching the RTK Query official pattern), responsive header/mobile nav, viewport/touch-target fixes found via audit — inserted ahead of feature work because it closes gaps in already-agreed `PRODUCT_GUIDE.md` requirements (responsive/accessible UI) that shipped incompletely in FDP-1–5 | ✅ Done |
| FDP-7 | `frontend-testing-infra` | Vitest + React Testing Library setup, component tests for the interactive/stateful UI kit primitives (Button, Modal, DropdownMenu, Tabs, Pagination, form controls) — closes the "component tests (UI kit)" commitment in `PRODUCT_GUIDE.md` that had no test framework in place at all | ✅ Done |
| FDP-8 | `visual-polish-branding` | Real UX review findings from live use of FDP-1–7: dark-mode contrast bug fixed at the token level (raw `neutral-*` classes never had a dark override — added `--color-secondary(-hover/-active)` semantic tokens, fixed every consumer), password show/hide toggle on `Input`, mobile nav rebuilt from a `Modal` to a proper slide-in `Drawer` (the old approach nested a portal-based `DropdownMenu` inside a `Modal` — z-index layering made the theme toggle inside it effectively unusable), custom themed scrollbar, footer, hand-designed logo mark + favicon | ✅ Done |
| FDP-9 | `menu-modifiers-ui` | Full `docs/PRODUCT_GUIDE.md` audit against everything shipped in FDP-1–8: menu item modifiers/options had backend schemas + frontend types since FDP-5 but **no UI on either side** — owner dashboard gets modifier group/option management (nested react-hook-form field arrays) plus a proper item-edit flow (item editing existed on the backend/RTK-Query layer but had no UI either), customer storefront gets a read-only modifier display. Also fixes stale docs (root `CLAUDE.md` still described the pre-2026-08-17 manual-PR-review workflow) and gives explicit roadmap ownership to requirements `PRODUCT_GUIDE.md` listed but no ticket claimed (see FDP-12 and FDP-22 below — FDP-22 was FDP-20 at the time this row was written, renumbered when FDP-15 split into FDP-15/16/17) | ✅ Done |
| FDP-10 | `cart` | Cart persistence (backend `Cart` schema/CRUD, one active restaurant per cart), customer-facing modifier selection UI (item detail modal — required/optional groups, quantity, notes), cart drawer (reusing FDP-8's `Drawer`). Split out of the original combined "cart-checkout" ticket — cart alone (backend + item-selection UI + drawer) is a full phase on its own; checkout/order-creation/promo-codes moved to FDP-11 so each PR stays reviewable | ✅ Done |
| FDP-11 | `checkout` | Checkout form (inline delivery address entry, delivery instructions, ASAP vs. scheduled — native `<input type="datetime-local">`, matching the native-input precedent already set for opening-hours time fields rather than a custom DatePicker component), promo/coupon code redemption (**new `PromoCode` schema** — gap found against `PRODUCT_GUIDE.md`'s "modern platform" bar, no other phase owns it), order creation (`Order` in `PENDING_PAYMENT` status per `docs/ARCHITECTURE.md` §3 — real payment processing is FDP-14, this phase only creates the order and resolves the default provider), order confirmation + basic order history list | ✅ Done |
| FDP-12 | `customer-account` | Standalone profile management (name/email/password/avatar) and saved-addresses list (reusing the `Address` shape FDP-11 introduces inline at checkout), **favorites/saved restaurants** — both listed in `PRODUCT_GUIDE.md` §4 but had no owning ticket until the FDP-9 audit added one | ✅ Done |
| FDP-13 | `orders-realtime` | Order state machine (status transitions beyond the `PENDING_PAYMENT` FDP-11 creates orders in), Socket.IO gateway, order tracking UI with status stepper, restaurant owner's live order queue (accept/reject/prepare/ready) | ✅ Done |
| FDP-14 | `payments` | Stripe/Paystack/Flutterwave adapters, provider resolver, webhooks, checkout payment UI — this is what actually moves an order out of `PENDING_PAYMENT` | ✅ Done |
| FDP-15 | `delivery-zones` | **New `DeliveryZone` schema** (restaurantId, radius/perKmFee/baseFee per `docs/ARCHITECTURE.md` §3), owner-managed zones in the dashboard, real distance-based delivery fee calculation replacing FDP-11's flat 10%-of-subtotal placeholder (falls back to the placeholder when neither the delivery address nor any zone has usable geo data, so checkout never breaks for an unzoned address). Split out of the original combined `delivery-rider-tracking` ticket — fee calculation, rider dispatch, and live map tracking are each substantial enough to be their own reviewable PR; see FDP-16/FDP-17 | ✅ Done |
| FDP-16 | `rider-dispatch` | **New `Rider` schema** (userId, vehicleType, isOnline, isVerified, rating — `docs/ARCHITECTURE.md` §3), rider dashboard (online/offline toggle, unassigned-`READY_FOR_PICKUP`-orders queue with self-assign accept, status updates through `ASSIGNED_TO_RIDER → PICKED_UP → OUT_FOR_DELIVERY → DELIVERED`, earnings/delivery history), the state-machine endpoints for those transitions (defined as graph edges since FDP-13 but never triggered by anything). Split from `delivery-rider-tracking`, see FDP-15 | ✅ Done |
| FDP-17 | `live-map-tracking` | Self-contained `<LiveDeliveryMap>` component behind `NEXT_PUBLIC_MAPBOX_TOKEN` (degrades to the existing status Stepper if the token is absent, per `docs/ARCHITECTURE.md` §10), rider location broadcast over the realtime gateway FDP-13 built, customer tracking page wired to show it live. Split from `delivery-rider-tracking`, see FDP-15/FDP-16 | ✅ Done |
| FDP-18 | `reviews-ratings` | Restaurant + rider reviews/ratings | ✅ Done |
| FDP-19 | `notifications` | Email + in-app + **SMS notifications (Termii)** | ✅ Done |
| FDP-20 | `admin-dashboard` | Admin approval workflows, dispute/refund handling, platform analytics, **promo code management UI** (creation is API-only from FDP-11 onward, same bootstrap pattern as restaurant approval before FDP-5's owner dashboard existed) | ✅ Done |
| FDP-21 | `search-performance-seo` | Search/filtering (cuisine/price/rating/delivery-time/sort — the backend DTO only supports text search + cuisine today), performance passes, SEO | ✅ Done |
| FDP-22 | `deployment-cicd` | Vercel + Render deploy configs, GitHub Actions CI/CD, final security review, **error-tracking hook (Sentry-ready)** — listed in `PRODUCT_GUIDE.md`'s Observability requirements but had no owning ticket until the FDP-9 audit added one; paired with deployment since that's when a real Sentry DSN gets wired into Vercel/Render env vars. **PWA manifest/installability confirmed OUT of scope by the user (2026-08-24)** — was a candidate addition pending confirmation; not built | ✅ Done |

FDP-23–29 were live-production bugfixes found through real usage after the FDP-22 deploy, not
new phases (see git history). FDP-30 onward is the "Glovo parity" initiative — a client review
compared the live app against glovoapp.com and asked for a phased plan to close the gap; the
full plan (stages, effort, external blockers, decisions made) lives in the roadmap artifact
shared 2026-08-28, summarized here as each ticket lands.

| FDP-30 | `menu-item-photos` | Image upload on the owner's menu-item form (reusing the existing `ImageUpload`/Cloudinary pipeline) + display on storefront item cards. The schema/type already supported `imageUrl`; only the UI was missing | ✅ Done |
| FDP-31 | `restaurant-card-images` | Restaurant logo/cover photo on the browse-restaurants grid, currently text-and-badges only | ✅ Done |
| FDP-32 | `homepage-sections` | Homepage rebuilt into Glovo-style sections: hero, "Top restaurants", "Anything delivered" category tiles, "Countries where we deliver" (generated from real restaurant `country` data), "Let's do it together". The hero's search box is keyword/cuisine search against the existing `/restaurants` endpoint, not a real address/geolocation input — true location-based discovery is the separately-tracked, not-yet-decided "nearby restaurants map" feature, so it isn't faked here. Folds in the Careers holding page and "Countries we deliver" originally scoped as FDP-34, since both are homepage sections the client explicitly asked for | ✅ Done |
| FDP-33 | `categories-page` | Multi-vertical category browse page (Food/Groceries/Pharmacy tabs) — Food fully functional, other verticals show "coming soon" pending FDP-56 | ✅ Done |
| FDP-34 | ~~`countries-careers-pages`~~ | Superseded — folded into FDP-32 | ➖ Merged into FDP-32 |
| FDP-35 | `checkout-ux-polish` | Checkout UX to match Glovo's pattern (delivery-option selector, collapsible promo code, cleaner summary) — arranges existing delivery-zone/promo-code functionality, no new backend logic | ✅ Done |
| FDP-36 | `overlay-elevation-dark-mode` | `Modal`, `Drawer`, `DropdownMenu`, and `Select`'s popup all used `bg-surface` instead of `bg-surface-raised` — in dark mode this is the identical near-black as the page background (`Card`/`Toast` already correctly use `bg-surface-raised`), so every one of these overlays visually blended into whatever was behind it. Found via a real screenshot of the add-to-cart modal in dark mode | ✅ Done |
| FDP-37 | `admin-create-restaurant-permission` | `POST /restaurants` was `@Roles('restaurant_owner')` only, while every other restaurant mutation (`update`, `toggle-open`, `approve`) already allows `admin` too, and the frontend's "Add a restaurant" page already gates itself on `["restaurant_owner", "admin"]` — an admin hit a real "no permission" error trying to use a page the UI itself said they could use | ✅ Done |
| FDP-38 | `fix-refresh-token-race-condition` | A real payment: pay via Paystack, get redirected back to `/checkout/callback`, land logged out with a bare "Unauthorized". Root cause: `SessionInitializer`'s proactive on-mount refresh never held `api.ts`'s shared mutex, so a page that *also* fires an authenticated query on mount (this was the first page in the app to do both at once — the homepage's own query is public) raced its own 401-triggered refresh against SessionInitializer's, and two concurrent calls against the single-use rotating refresh token trip reuse-detection and revoke the whole session | ✅ Done |
| FDP-39 | `fix-refresh-deadlock` | FDP-38's own fix shipped a self-deadlock: `refresh()` routes through the same `baseQueryWithReauth` it was being made to hold the mutex around, so `SessionInitializer` acquired the lock then blocked forever waiting on its own `await mutex.waitForUnlock()` inside that call — hanging every request on every page. Caught by testing the fix locally against the real backend before trusting it, not assumed from reasoning alone. Fixed by skipping the wait specifically when the request *is* `/auth/refresh` itself | ✅ Done |
| FDP-40 | `item-images-everywhere` | Menu item photos were missing from the cart drawer, checkout summary, and order detail page — cart/order items snapshot `name`/`price` at add-time but never `imageUrl`, so there was nothing to show even though FDP-30 added the photo itself. Added the snapshot field to both, plus a thumbnail in the admin restaurant-review page (FDP-29), which had item cards but no image despite being the one page whose whole purpose is visually reviewing a menu | ✅ Done |
| FDP-41 | `phone-otp-auth` | Phone number sign-up/login with SMS OTP via the existing Termii integration. Email/password stays the primary account identity — phone is a verified, optional supplement (see docs/ARCHITECTURE.md §11) — signing up with phone-only and no email is a bigger structural change (email is assumed to exist everywhere else: receipts, password reset, admin lists) not attempted here. WhatsApp OTP is a fast-follow once a Meta-approved WhatsApp Business sender exists | ✅ Done |
| FDP-42 | `google-oauth` | "Continue with Google" — server-driven Passport redirect flow, not Google's client-side JS SDK. Redeems a short-lived one-time exchange code through the frontend's own `/api/*` proxy rather than setting the refresh cookie directly from the callback (which lands on the backend's bare domain, third-party again) — see docs/ARCHITECTURE.md §11 | ✅ Done |
| FDP-43 | `facebook-oauth` | "Continue with Facebook" — reuses every part of FDP-42's pattern (`AuthService.loginOrRegisterWithOAuthProfile` is provider-agnostic). The code ships regardless of Meta's business verification timeline; going live on the real production domain (not just localhost) still needs that review to finish, independent of anything here | ✅ Done |
| FDP-44 | `facebook-oauth-email-scope-fix` | Real-world Facebook login was failing with a 500 on `/auth/facebook/callback` — unlike Google, Facebook doesn't return an email by default and `FacebookStrategy` never requested the `email` permission scope, so `profile.emails` was always empty and `validate()` threw. Also hardened both Google's and Facebook's "no email on profile" error path to a proper `UnauthorizedException` (401) instead of a raw `Error` surfacing as an opaque 500 | ✅ Done |
| FDP-45 | `dark-mode-badge-contrast` | Cuisine-type badges (and other `Badge variant="primary"` uses: homepage category-tile icons, categories-page cuisine chips) were nearly illegible in dark mode — `--dark-primary` text on `--dark-primary-subtle` background measures ~3.05:1, failing WCAG AA's 4.5:1 for normal text despite a numeric lightness gap, because both are the same saturated hue. Added a dedicated `--color-primary-subtle-foreground` token (brand-300 in dark mode, ~7.5:1) instead of widening `--dark-primary` itself, to avoid touching that token's other consumers (links, focus rings) | ✅ Done |
| FDP-46 | `homepage-redesign-nav-search` | Homepage read as a near-literal copy of a competitor's layout — rearranged section order/visual treatment (two-column hero with a decorative floating order-status card, gradient wash + wave-curve backgrounds instead of flat alternating bands, "Countries we deliver" folded into a slim strip instead of its own section) while keeping the same functional sections. Moved restaurant/cuisine search out of the homepage hero into the persistent header (`HeaderSearch`, `layout.tsx`) so it's reachable from every page — two-row responsive layout below `sm`. Also fixed `/restaurants` silently ignoring its own `?search=` query param, which the homepage's search had been linking to all along | ✅ Done |
| FDP-47 | `homepage-brand-overhaul` | FDP-46's redesign still read as a re-skinned template (same stacked "hero → grid → grid → 3 cards" shape as most delivery-app clones, just with gradients added). Replaced it with a structurally different composition: a solid always-dark diagonal-cut brand hero (not a soft neutral wash) with a floating order-status card straddling the angled edge; an asymmetric bento grid merging category tiles with two live platform stats (real restaurant count + real average delivery time, not filler numbers) instead of two separate equal-tile grids; "Top restaurants" as a horizontal snap-scroll rail (and moved directly under the hero, ahead of the category grid, so it's immediately reachable); and the closing "Let's do it together" as one wide gradient banner with inline items instead of three matching bordered cards | ✅ Done |
| FDP-48 | `sticky-footer-viewport-fallback` | On short-content pages (admin dashboard tabs, empty states), the footer appeared with dead space below it instead of pinned to the viewport bottom — but only in some embedded webviews (observed in VS Code's Simple Browser panel), not in real Chrome, where the root layout's flex-column sticky-footer was already measuring correctly (verified live: footer's bottom edge exactly equals `window.innerHeight`, zero extra scroll, at the exact viewport size from the report). Root cause: body relied solely on the `min-h-dvh` Tailwind utility, and some engines don't size `100dvh` to the actually-visible viewport. Replaced it with a manual two-declaration CSS fallback in `globals.css` (`min-height: 100vh` then `100dvh`) — a Tailwind class can't be overridden from plain CSS due to specificity, so the utility was removed from `layout.tsx` and this is now the only source of the rule | ✅ Done |
| FDP-49 | `payment-verification-fallback` | Orders were stuck on `PENDING_PAYMENT` forever and the checkout callback page hung on "Confirming your payment…" indefinitely — the only path out of `PENDING_PAYMENT` was a provider webhook, and webhook delivery apparently never reached this deploy for real transactions. `PaymentAdapter.verify(reference)` was already fully implemented for all three providers but had no caller anywhere. Added `PaymentsService.verifyPayment()` + `POST /payments/:orderId/verify`, called actively from the checkout callback page right after the provider redirects back, reusing the same idempotent `markPaidFromWebhook`/`markPaymentFailed` transitions the webhook path uses (whichever of the two arrives first wins). This was also the root cause of two other reported gaps: reviews only unlock at `DELIVERED` (by design) and a restaurant owner's order queue only shows `PLACED`-or-later orders (by design) — both were correctly implemented already but functionally unreachable, since no order had ever left `PENDING_PAYMENT` | ✅ Done |
| FDP-50 | `order-detail-payment-recheck` | FDP-49 fixed *new* checkout runs but did nothing for orders already stuck on `PENDING_PAYMENT` from before the fix — the order detail page had no way to re-trigger a check, and its `PENDING_PAYMENT` banner said "waiting on payment integration" (stale copy from before FDP-14 shipped real providers, actively misleading). Replaced it with a "Check payment status" button wired to the same `verifyPayment` endpoint, so an already-stuck order can be resolved retroactively without repeating checkout | ✅ Done |
| FDP-51 | `payouts-foundation` | Vendor payouts epic, part 1 of 4 — platform commission % + data model foundation, no real money movement yet. Adds `PLATFORM_COMMISSION_RATE` config, snapshots `platformFeeAmount`/`restaurantPayoutAmount` on every order at creation time (so a later rate change never rewrites historical orders), adds `Restaurant.payoutAccounts` (per-provider onboarding status/reference, all `pending` until FDP-52-54 wire up real onboarding), and a restaurant dashboard "Earnings" view (gross/fee/net, all real numbers) with a "Payout setup required" banner in place of a withdraw button | ✅ Done |
| FDP-52 | `paystack-subaccount-payouts` | Vendor payouts epic, part 2 of 4 — Paystack subaccount onboarding: a restaurant picks their bank + account number, the backend resolves it to a name via `/bank/resolve` for them to confirm, then creates a subaccount via `/subaccount`. At charge time `PaystackAdapter.initiate()` adds `subaccount` plus an exact `transaction_charge` computed per-order as `total - restaurantPayoutAmount` — deliberately *not* the subaccount's own stored `percentage_charge`, since a flat percentage of the whole order total would hand the restaurant a cut of the delivery fee and service fee too. First provider tackled because it covers this project's primary test currency (NGN). Live-verified end-to-end against the real Paystack sandbox (bank list, account resolution, subaccount creation, and a split `/transaction/initialize` call) in addition to unit coverage | ✅ Done |
| FDP-53 | `flutterwave-subaccount-payouts` | Vendor payouts epic, part 3 of 4 — same subaccount-and-split pattern as FDP-52, for Flutterwave: bank list, account resolution, subaccount creation on `FlutterwaveAdapter`, `FlutterwavePayoutsController` mirroring `PaystackPayoutsController`, and a `FlutterwavePayoutSetup` panel on the earnings page shown alongside Paystack's for currencies that support both. Live-verified banks/resolve/subaccount-creation against the real Flutterwave sandbox before writing any adapter code, since two real differences from Paystack's shape weren't documented anywhere in this codebase and getting either wrong would misroute real money: Flutterwave's split lives in a `subaccounts` array (not a single `subaccount` field), and its `flat_subaccount` charge type is the *inverse* of Paystack's `transaction_charge` — the flat amount goes to the subaccount (restaurant), not the platform — confirmed against Flutterwave's own split-payments docs. Also: no ×100 (Flutterwave's amounts are already in major units), `split_value` is a 0–1 fraction not Paystack's 0–100, and subaccount creation needs a `business_email` pulled from the restaurant owner's account since restaurants don't have their own email field | ✅ Done |
| FDP-54 | `stripe-connect-payouts` | Vendor payouts epic, part 4 of 4 — Stripe Connect (Express accounts) for card/global payments: hosted onboarding link, an `account.updated` webhook to know when onboarding actually completes, and `transfer_data`/`application_fee_amount` on the Checkout Session. The most involved of the three providers since Connect's onboarding is a multi-step redirect flow, not a single API call like the other two | ⏳ Planned |
| FDP-55 | `i18n-french` | i18n framework + French as the first additional language | ⏳ Planned |
| FDP-56 | `grocery-pharmacy-marketplace` | New `Store`/`Product` data model + non-restaurant checkout for a real grocery/pharmacy marketplace — comparable in size to the original FDP-5 restaurant system; scoped separately before starting, not a line item of FDP-33 | ⏳ Needs separate scoping |
| FDP-57 | `paystack-payout-error-handling` | Connecting a Paystack payout account failed with a raw "Internal server error" — `PaystackPayoutsController`'s three endpoints never caught a raw adapter error the way `PaymentsService.initiatePayment`/`refundOrder` already do, so any real Paystack rejection (an unresolvable account number, a bank code mismatch) fell straight through to Nest's global filter as an opaque 500. The actual trigger live: Paystack's test mode caps *real* bank-account resolution at 3/day — exhausted by this session's own FDP-52 verification calls — which Paystack reports clearly, but the missing try/catch swallowed that message entirely. Fixed the error handling, and added Paystack's documented workaround (bank code `001`, "Test Bank") as a selectable option in the bank dropdown whenever running on a test-mode key, so payout setup can still be exercised without waiting on the daily cap | ✅ Done |
| FDP-58 | `searchable-selects-and-confirm-dialogs` | Two platform-wide UX gaps: (1) long-list `Select` dropdowns (194 countries, 142 currencies, 280+ Paystack banks) had no way to filter — just scrolling — so `Select` gained an opt-in `searchable` prop (a type-to-filter box, replacing single-character typeahead for exactly the lists where typeahead doesn't scale), applied to the restaurant form's country/currency selects and the Paystack bank select; (2) every destructive action in the app (delete saved address, delete delivery zone, delete menu category/item, reject/cancel a restaurant's order, admin issue-refund) fired immediately on click with no confirmation — added a reusable `ConfirmDialog` (wraps the existing `Modal`) and wired it into all six | ✅ Done |
| FDP-59 | `payment-security-hardening` | A real audit pass on the payment/payout system prompted by an explicit "must be hacker-proof" ask: added rate limiting to `/payments/initiate`, `/payments/:id/verify`, `/payments/:id/refund`, and the Paystack payout onboarding endpoints (none had any beyond the app-wide 100/min default); added a fraud-detection email to a restaurant owner every time their payout account is connected or changed (`payout_account_changed` notification type) — since silently redirecting a restaurant's future payouts is exactly what an attacker with a compromised owner account would do, and previously nothing would have surfaced it. Confirmed already solid from earlier work and left unchanged: webhook signature verification on all 3 providers, no client-trusted payment-status writes, ownership checks on every order/payout mutation, no client-supplied charge amounts, and `trust proxy` set correctly (not `true`) so per-IP throttling can't be defeated by a spoofed `X-Forwarded-For` | ✅ Done |
| FDP-60 | `restaurant-kyc-approval-gate` | Restaurant onboarding had no proof-of-business check at all — anyone could list a restaurant with just a name and address. Added a required `complianceDocumentUrl` (CAC certificate or a country's equivalent business-registration proof, uploaded via a new `DocumentUpload` component to a `compliance-documents` Cloudinary folder, PDF or image) to restaurant creation, and hardened the approval gate so it can't be bypassed from either side: `RestaurantsService.approve()` now unconditionally rejects if no document is on file, and a new `AdminService.approveRestaurant()` (replacing the old `RestaurantsController`-owned approve endpoint, now `PATCH /admin/restaurants/:id/approve`) additionally requires at least one menu item before delegating to it — so an admin can no longer approve a restaurant with a missing document or an empty menu even by hitting the API directly. The admin review page surfaces the uploaded document (or its absence) and disables the Approve button client-side with an explanation of which prerequisite is missing, mirroring the server-side gate for immediate feedback | ✅ Done |
| FDP-61 | `rider-kyc-verification` | Rider onboarding was a single "pick your vehicle" form — no identity, licensing, or accountability checks at all before someone could accept deliveries. Made `POST /riders/apply` collect a full modern dispatch-rider KYC packet up front: date of birth (18+ enforced server-side), a government ID (type + number + document), proof of address, and — for any motorized vehicle type — a driver's license (number, expiry, document), vehicle plate number, and vehicle registration document; plus a named guarantor/surety (full name, phone, relationship, address) and a separate next-of-kin emergency contact. `RidersService.verify()` re-checks completeness unconditionally regardless of caller, the same posture as FDP-60's restaurant gate, guarding against legacy data predating this schema. The admin riders list now links to a full per-rider review page (`/admin/riders/:id`) showing every field and document before the verify decision, instead of a blind one-click "Verify" button, and disables verification client-side with an explanation when anything required is missing. New `rider-documents` Cloudinary folder alongside FDP-60's `compliance-documents` | ✅ Done |
| FDP-62 | `document-upload-ux-and-rider-cta-fixes` | Follow-up fixes reported after FDP-60/61 shipped: (1) `DocumentUpload` now shows a real preview — an actual image thumbnail for JPG/PNG, a file icon for PDF — plus the real filename (captured from the browser `File` during upload; falls back to decoding the Cloudinary public_id when reloading an existing document), a persistent "PDF, JPG, or PNG — max 5MB" hint shown up front instead of only after a rejected file, and the limit itself lowered from 10MB to the stated 5MB; (2) root-caused admin's "this page isn't working" 401 when opening an uploaded compliance/rider document to Cloudinary's account-level "PDF and ZIP file delivery" restriction — live-verified against the real account that neither signing nor `type: authenticated` delivery bypasses it (`X-Cld-Error: deny or ACL failure` in all cases; a `.png` delivered the identical way returns 200 fine), so this needs a one-time Cloudinary Console setting change ("Allow delivery of PDF and ZIP files" under Settings → Security) on the account owner's end, not a code fix — **user enabled it 2026-09-02, confirmed documents now load correctly in production**; (3) the homepage "Let's do it together" rider card and the footer's "Become a rider" link were shown to `admin`/`restaurant_owner` accounts, which `RidersService.apply()` always rejects — both now hide the rider CTA entirely for those two roles instead of pointing them at an application that 400s | ✅ Done |
| FDP-63 | `skill-and-memory-continuity-update` | Housekeeping after the long FDP-49→62 payouts/KYC/hardening epic: refreshed the `new-feature-branch` skill to document how tickets actually got created this epic (reactively, from live bug reports — no pre-existing roadmap row to look up — rather than only from pre-planned phases), and updated Claude's cross-session memory with everything shipped, the resolved Cloudinary finding, and what's still open (nearby-restaurants-map, FDP-53/54/55/56 planned-but-unbuilt, the local-only stale `MONGODB_URI` in `backend/.env`) | ✅ Done |
| FDP-64 | `restaurant-sales-report-cogs` | New restaurant-facing detailed sales report + cost-of-goods-sold (COGS) feature, requested directly by the user rather than from a pre-existing roadmap gap. Scoping decisions (user-confirmed): per-item cost price (not a restaurant-level %), forward-only COGS (no retroactive estimate on historical orders), CSV export over charts. Adds `MenuItem.costPrice` (owner-only, never customer-facing) and snapshots it onto `OrderItem.costPrice` at order-creation time — both nullable, since existing items/orders predate the field and a null is treated as "unknown," never silently as "free." Also adds `Order.deliveredAt` (indexed), set once by `OrdersService.updateStatusByRider` on the `DELIVERED` transition, since date-range sales reporting needs a real matchable field rather than unwinding `statusHistory` on every query. New `OrdersService.getSalesReport`/`getSalesReportOrders` (date-range filterable, `restaurant_owner`/`admin`-gated like `getEarningsSummary`) power a `GET .../sales-report` totals+by-item+by-day breakdown and a `GET .../sales-report/export` order-level CSV download (hand-rolled RFC 4180 escaping, no new dependency). Items with incomplete cost data are surfaced by name in both the report and the menu list (a "No cost price set" badge), rather than silently understating profit. New `/dashboard/restaurants/:id/sales-report` page; the CSV download is the app's first Blob-based file download, needed since the access token lives in Redux state, not a browser-readable cookie | ✅ Done |
| FDP-65 | `payment-system-robustness-audit` | User asked for the Stripe/Paystack/Flutterwave payment system to be audited for bugs and hardened. Ran the `code-review` skill at max effort against `backend/src/payments/**` and the payment-relevant parts of `backend/src/orders/`; all 15 findings were real, verified money/security bugs and all were fixed: (1) `PaymentsService.handleWebhook` never checked that a webhook's order actually belongs to the provider whose route received it — a correctly-signed webhook on the *wrong* provider's route could mark another provider's order paid with no charge ever collected; (2) `FlutterwaveAdapter.handleWebhook` never checked the event type at all (unlike Stripe/Paystack) and (3) could throw an uncaught `TypeError` on a malformed body — both fixed, plus a `PaymentsService`-level try/catch as defense in depth for any adapter; (4) `refundOrder` only ever allowed refunding a `DELIVERED` order, so a restaurant cancelling an already-paid order (`PLACED`/`ACCEPTED_BY_RESTAURANT`/`PREPARING` → `CANCELLED`) had no way to ever reverse that charge — added a `CANCELLED` → `REFUNDED` state-machine edge and `REFUNDABLE_STATUSES`; (5) `PaystackAdapter.refund()` discarded the API response entirely and (6) `FlutterwaveAdapter.refund()` silently returned/never checked its own response — both could mark an order refunded when Paystack/Flutterwave actually rejected the refund; (7) refunds had no concurrency guard — replaced the single-step `OrdersService.markRefunded` with an atomic `claimForRefund`/`finalizeRefund`/`revertFailedRefundClaim` three-phase flow (`findOneAndUpdate`'s atomicity is the lock) so two near-simultaneous refund attempts can't both fire a live provider call; (8) `setPaymentRef` overwrote the order's single `paymentRef` on every retry, orphaning an earlier checkout session a customer might still complete — added `Order.paymentRefs: string[]` and match against it; (9) `markPaidFromWebhook` was a non-atomic findById-then-save racing the client's `verifyPayment` poll by design — converted to an atomic `findOneAndUpdate` compare-and-swap on `status: 'PENDING_PAYMENT'`; (10) `markPaymentFailed`'s idempotency guard didn't cover `paymentStatus: 'refunded'`, letting a late webhook downgrade a refunded order; (11) a promo discount could push `order.total` below `order.restaurantPayoutAmount`, asking Paystack for a negative `transaction_charge` or a Flutterwave subaccount to receive more than the whole charge — `PaymentsService.initiatePayment` now clamps the split amount to `order.total`; (12) the sales-report CSV export (FDP-64) was vulnerable to CSV/formula injection via a menu item name starting with `=`/`+`/`-`/`@` — `csvField` now neutralizes it; (13) `round2`'s `Math.round(v*100)/100` silently misrounded a real fraction of monetary values due to IEEE-754 float imprecision (e.g. `1.5*0.15` → 0.22 instead of 0.23) — fixed with `+ Number.EPSILON` in both the `orders` and `delivery-zones` copies; (14) `markRefunded` had no self-guard against a future second caller bypassing the state machine — resolved by the claim/finalize/revert restructure itself; (15) the sales report's date-only `to` query param (from a native `<input type="date">`) parsed to midnight UTC and silently excluded the entire last day of a range — fixed with a date-only-aware `parseRangeTo` helper. All fixes covered by new/updated unit tests (adapter-level response-checking and event-filtering tests, concurrency tests proving only one of two concurrent calls wins, provider-mismatch and malformed-payload webhook tests) | ✅ Done |
| FDP-66 | `header-logo-nav-and-homepage-carousel` | Two user-reported UX bugs, fixed together: (1) the header's logo previously linked to `/restaurants` with "Restaurants" text baked into the same link — there was no way to get back to the homepage at all. The logo (`layout.tsx`) is now a standalone link to `/`, and "Restaurants" is a normal nav link in its own `<nav>` next to it (`hidden sm:flex`, same visibility pattern as the existing `AuthStatus`/`ThemeToggle` group) — `MobileNav`'s drawer already carried an equivalent "Restaurants" link separate from any logo, so mobile needed no structural change beyond the shared logo fix; (2) the homepage's "Top restaurants" section was a manually-scrollable snap-scroll rail with no actual motion — replaced with a new reusable `Carousel` UI-kit component (`components/ui/carousel.tsx`): native scroll-snap + touch scrolling under the hood (so drag/swipe/keyboard scrolling work for free) with a `setInterval` auto-advance layered on top, prev/next buttons that disable at each edge, pauses on hover/focus/touch, and fully respects `prefers-reduced-motion` (no auto-advance at all). Covered by new component tests (edge-disabling, auto-advance, wrap-around, hover-pause, reduced-motion) following the same jsdom-layout-mocking pattern as `dropdown-menu.test.tsx` | ✅ Done |
| FDP-67 | `fix-carousel-test-typecheck-and-deploy` | User reported FDP-66's header/carousel fixes weren't visible live. Root cause was two-fold: (1) `carousel.test.tsx` assigned a plain `vi.fn()` directly to `track.scrollBy`/`track.scrollTo`, which type-checks fine under a bare local `tsc --noEmit` but fails under Vercel's `next build` TypeScript pass — `Element.scrollBy`/`scrollTo` are overloaded (`(options?) => void \| (x, y) => void`), and the mock's narrower signature isn't assignable to the full overloaded type; fixed by casting to `Element["scrollBy"]`/`Element["scrollTo"]` at assignment, verified against a from-scratch `rm -rf .next && npm run build` (not just the incrementally-cached one that had passed locally before); (2) discovered mid-investigation that Vercel is **not** auto-deploying on push to `main` at all — the live frontend was 19+ hours stale, predating not just FDP-66 but FDP-64 and FDP-65 too, while the Railway backend *does* auto-deploy correctly and was already current (confirmed via existence-not-404 checks on FDP-53/64 routes). Fixed by manually running `vercel --prod` from the repo root once the typecheck bug was fixed. **The missing Vercel auto-deploy is not fixed by this ticket** — every future frontend-only merge to `main` will need the same manual `vercel --prod` push until the GitHub integration is reconnected/repaired on the Vercel dashboard (outside what a git push can fix) | ✅ Done |
| FDP-68 | `carousel-smooth-ticker-motion` | User feedback after seeing FDP-66/67's carousel live: the periodic `setInterval` + big `scrollTo`-jump-every-4s motion read as fast and jarring, "not appealing." Replaced with a continuous, slow `scrollLeft` drift (a news-ticker-style glide) — a `setInterval` tick every 30ms nudges `scrollLeft` by a small `speed`-derived amount (default 30px/s) rather than jumping ~85% of the visible width all at once; wraps back to 0 on reaching the end. Removed `snap-x`/`snap-mandatory` from the track — scroll-snap actively fights a slow continuous programmatic drift (each tiny step gets corrected back by the browser's own snap behavior), so this trades snap-to-card for a smooth glide; manual prev/next buttons are unaffected (still a discrete `scrollBy` page-jump on click). `Carousel`'s `autoAdvanceMs` prop renamed to `speed` (px/second) to match the new continuous model — the only caller (`page.tsx`'s "Top restaurants" rail) uses the default, so no call-site changes needed. Test suite rewritten to assert the actual rate (small movement per tick, not a big periodic jump) rather than just "a scroll call happened" | ✅ Done |

Phases are sequential but not rigid — if a later phase's work is discovered while doing an
earlier one, note it here rather than scope-creeping the current branch.
